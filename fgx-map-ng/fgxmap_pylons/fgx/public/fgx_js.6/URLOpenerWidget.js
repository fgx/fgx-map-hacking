
Ext.namespace("FGx");

FGx.URLOpenWidget = Ext.extend(Ext.Panel, {

//===========================================================
//== Grid
constructor: function(config) {
	config = Ext.apply({
		iconCls: config.icoCls,
		
		
			
		
		
	}, config);
	FGx.URLOpenWidget.superclass.constructor.call(this, config);
}, // Constructor	


});
